@{
    ViewBag.Title = "Index";
}

@if (Session["user"] != null)
{
    <h2>HOŞGELDİNİZ...</h2>
    @Html.ActionLink("Çıkış Yap", "Logout")
}
else
{
    <p><font color="red">BİR PROJE DAHA BAŞARIYLA BİTİRİLDİ</font></p>
}